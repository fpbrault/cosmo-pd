{
	"name": "@cosmo/cosmo-synth-engine",
	"private": true,
	"version": "0.4.0",
	"license": "GPL-3.0-only",
	"type": "module",
	"scripts": {
		"build:wasm": "bun run build:wasm:release",
		"build:wasm:release": "cross-env RUSTFLAGS='-D warnings' cargo check -p cosmo-synth-engine --lib --bins && cross-env RUSTFLAGS='-D warnings' cargo check -p cosmo-synth-engine --features wasm && cross-env CARGO_TARGET_DIR=target wasm-pack build . --release --target no-modules --out-dir ../cosmo-pd101/site/public/cosmo-synth-engine-wasm --features wasm && rimraf ../cosmo-pd101/site/public/cosmo-synth-engine-wasm/.gitignore",
		"perf:engine:hotspots": "cargo run --release --bin render-bench -- --suite hotspots --json"
	}
}
