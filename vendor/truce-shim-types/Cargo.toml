[package]
# PATCH: vendored alongside truce-au so the local AU override stays
# self-contained instead of relying on upstream workspace-inherited metadata.
name = "truce-shim-types"
version = "0.45.4"
repository = "https://github.com/truce-audio/truce"
homepage = "https://github.com/truce-audio/truce"
edition = "2024"
license-file = "../../LICENSE"
rust-version = "1.90"
description = "Shared C header types for the truce AU shim"

[lints]
workspace = true
